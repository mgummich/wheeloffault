{
  "date": "2026-09-08",
  "checkedAgainstCommit": "47149beddf57de29488104bc5095d23875b61391",
  "branch": "main",
  "environment": {
    "node": "v26.8.1",
    "pnpm": "11.24.0",
    "os": "darwin (Darwin 25.6.0)"
  },
  "note": "This pass has uncommitted changes on top of checkedAgainstCommit (docs/en/motion.md, docs/de/motion.md, SECURITY.md, ARCHITECTURE.md, docs/de/architektur.md, scripts/build-docs.mjs, this file); see `git diff --stat` for the current shape rather than a snapshot recorded here, since that snapshot goes stale the moment it's committed. Changes: (1) docs/en/motion.md and docs/de/motion.md §4: the `windup` anticipation dip was documented as '~3.5°', off by ~18x from the real figure computed from ease()'s -0.035 fraction applied to the >=1800° travel in Wheel.tsx — corrected to '~3.5% of travel, ~63° over windup's ~1800° sweep' with a comparison note for scale. (2) Same files §2: the duration-token table listed ranges ('150-180ms', '240-400ms') for tokens that are single fixed values in src/web/styles.css (160ms, 400ms) — corrected to the actual values. (3) SECURITY.md's Origin bullet read as if it applied to every request; corrected to note it only runs for state-changing methods (http.ts's checkOriginAndHost returns early for GET before reaching the Origin comparison). (4) SECURITY.md's `GET /api/teams` bullet said unconditionally unauthenticated; corrected to note it's gated like other /api/* routes when SCHULDRAD_PASSWORD is set. (5) ARCHITECTURE.md and docs/de/architektur.md: fixed the nonexistent `addMember` to `addMembers`, and corrected the `(state, input) → Event[]` shape, since commitSpin/revealSpin (src/domain/fairness/spin.ts) are async and return Promise<CommitResult>/Promise<DomainEvent[]>. (6) scripts/build-docs.mjs: closed the vacuous prefix-match in checkContentLanguage's body.includes assertion (an index target's href, e.g. '../de/', was a prefix of every other same-language href on the page, so a broken/wrong-language index link could pass undetected) — now requires an exact quote-closed or fragment-continued match; the stale ponytail: comment claiming this was a deliberate safe pattern was removed and replaced with an accurate one. (7) scripts/build-docs.mjs: checkContentLanguage previously resolved link targets by basename only, so a correct-basename link in the wrong directory (e.g. root README.de.md linking `deployment.md` instead of `docs/de/deployment.md`) would build green and 404 on GitHub; added a stat() against the literal relative path from the source file's own directory. (8) scripts/build-docs.mjs: images remain entirely unvalidated (no image() renderer override, collectLinks() only visits link nodes, checkArtifactLinks() only scans <a href>) — documented in place as a ponytail: known ceiling rather than built, since no docs source embeds an image today.",
  "checks": [
    {
      "name": "format:check (pnpm format:check)",
      "status": "pass",
      "details": "Checked 93 files. No issues."
    },
    {
      "name": "lint (pnpm lint)",
      "status": "pass",
      "details": "Checked 95 files. 0 errors, 6 warnings, all pre-existing CSS lint warnings in src/web/styles.css (prefers-reduced-motion !important overrides and one noDescendingSpecificity pair), unrelated to this pass's changes. Exit 0."
    },
    {
      "name": "typecheck (pnpm typecheck)",
      "status": "pass",
      "details": "tsc -p tsconfig.json --noEmit: no output, exit 0."
    },
    {
      "name": "test:unit (pnpm test:unit)",
      "status": "pass",
      "details": "16 test files, 97 tests passed, 0 failed."
    },
    {
      "name": "test:integration (pnpm test:integration)",
      "status": "pass",
      "details": "5 test files, 50 tests passed, 0 failed."
    },
    {
      "name": "build (pnpm build)",
      "status": "pass",
      "details": "vite build succeeded, then scripts/build-docs.mjs's completeness check, content-language check (including the new exact-match assertion and the new per-link stat() resolution), and artifact-wide link/anchor check all passed against the real docs tree (8 registry entries, 15 rendered doc pages since SECURITY.md is enOnly, plus the /docs/ redirect)."
    },
    {
      "name": "build:server (pnpm build:server)",
      "status": "pass",
      "details": "vite build --mode server succeeded."
    },
    {
      "name": "e2e (pnpm e2e)",
      "status": "pass",
      "details": "3 Playwright tests passed across 2 projects (static PWA offline-reload session test, server-mode team sharing across browsers, core Schuldrad journey). ~5s."
    },
    {
      "name": "build-docs.mjs mutation testing (scratch copy under /private/tmp, not the repo)",
      "status": "pass",
      "details": "Full repo rsync'd to a scratch dir. Baseline build confirmed green (exit 0), then 13 mutations were each applied in isolation (from a clean copy of the fixed source) and rebuilt directly with `node scripts/build-docs.mjs`, and one valid-link control was checked for no false positive. All 13 mutations failed the build (exit 1) as expected; the control passed (exit 0); a final restore-and-rebuild sanity check passed (exit 0). Mutations: (a) renderer language-fallback forced to 'en' — failed. (b) #en/#de fragment marker disabled — failed. (c) directory-marker regex anchored to string start instead of any path segment — failed. (d) dead .md link added — failed via the artifact existence check. (e) dead non-.md link added — failed via the same check. (f) navHref's slug interpolation broken — failed, every nav tab on every page. (g) enOnly language-switch fallback reverted — failed, re-emitting a href to docs/de/security.html which was never written. (h) README.de.md's own #en marker inverted to #de — failed via the content-language pairing check. (i) dead in-page anchor added — failed via the heading-id check. (j, item 6 regression) index-link resolution forced to always render 'en/' regardless of computed language, isolated from any other same-language href on the page — failed via the newly-exact-matched body.includes assertion (this is exactly the case the old vacuous prefix match let through). (k, item 7 regression) a root README.de.md link with the right basename ('deployment.md') but the wrong directory (real file is docs/de/deployment.md) — failed via the new stat() check. Control: docs/en/deployment.md#environment-variables, a real cross-doc deep link, built clean (no false positive); a second dead-anchor variant (deployment.md#no-such-section-at-all) was also tried and correctly failed, confirming the existing cross-document fragment check still works."
    },
    {
      "name": "docker: sqlite profile smoke test",
      "status": "previously-verified",
      "details": "Not re-run in this pass; this pass touched documentation and the docs build script only, no server/container code.",
      "verified": "previously verified, not re-run in this session"
    },
    {
      "name": "docker: overengineered profile (Postgres + Redis) build + smoke test",
      "status": "previously-verified",
      "details": "Not re-run in this pass; no server/container code changed.",
      "verified": "previously verified, not re-run in this session"
    },
    {
      "name": "docker: auth-enabled container smoke test (SCHULDRAD_PASSWORD set)",
      "status": "previously-verified",
      "details": "Not re-run in this pass; no server/container code changed.",
      "verified": "previously verified, not re-run in this session"
    }
  ]
}
